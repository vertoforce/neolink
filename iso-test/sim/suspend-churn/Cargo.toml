# Standalone: its own [workspace] so it never perturbs the neolink workspace.
[package]
name = "suspend-churn"
version = "0.1.0"
edition = "2021"
publish = false

[workspace]

[dependencies]
glib = "0.20"
gstreamer = "0.23.0"
gstreamer-rtsp-server = { version = "0.23.0", features = ["v1_20"] }
